version: '3.8'

services:
  tvproxy:
    image: ghcr.io/yufanpin/tvproxy:latest
    container_name: tvproxy
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - /etc/localtime:/etc/localtime:ro   # 自动使用宿主机时区
    restart: unless-stopped
